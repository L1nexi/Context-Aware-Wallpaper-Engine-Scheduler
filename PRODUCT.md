# Product

## Register

product

## Users

用户是 Context Aware WE Scheduler 的使用者。他们通常已经完成基础配置，并通过托盘菜单观察当前状态。

用户会在这些时刻打开诊断页：

- 右键菜单的当前匹配显示为语义池，例如“雨落时分（+1）”。
- 当前匹配与自己的体感不一致。
- 只是想观察调度器过去一段时间的运行状况。

他们的核心任务不是重新判断当前场景的 Winner，而是快速理解最近一段时间系统感知到了什么、播单得分如何变化、调度为什么执行或被阻止。

## Product Purpose

本项目是一个具备多维上下文感知能力、防打扰调度策略的 Wallpaper Engine 调度器。它读取时间、季节、天气、窗口活动等上下文，并通过播单池、语义连续性和防打扰规则，在尽量维持用户心流的前提下调度壁纸。

诊断页的目的，是把过去一段时间的运行事实优雅地展示出来。它应帮助用户快速理解系统过去一段时间的运行情况：系统看到了什么、策略如何起作用、各个播单分数怎样变化、为什么发生切换或为什么没有切换。

成功的诊断页应让用户在几十秒内理解：

- 天气和窗口活动策略的微妙触发情况
- 多个候选播单的得分趋势
- 当前调度行为是执行、保持、轮播、暂停，还是被某个规则阻止
- 如果没有切换，原因是 idle、CPU、全屏、冷却还是语义连续性等因素

## Brand Personality

亲和、清晰、智能感、现代。

这是一个极客向工具，可以保留适度的数据密度和技术气质。但界面仍应优先服务理解，而不是堆叠原始调试字段。

## Anti-references

- 不做成管理后台或工作台。
- 不做成运维大屏。
- 不做成 Winner 排行榜。
- 不把 similarity / similarity gap 当作核心展示对象。
- 不满屏展示 Raw Tags、Resolved Tags 和内部细节。
- 不恢复旧 dashboard / workbench 心智。
- 不把诊断页扩展成配置编辑器、历史分析产品或调参工具。

## Design Principles

1. 展示运行事实，而不是替用户重新判定 Winner。
2. 优先解释最近一段时间的变化，而不是只解释当前瞬间。
3. 把 Policy 从原始标签输出转换成用户能感知的事实：天气级别、活动匹配、触发进程和标题。
4. 把 Match 展示为多个播单在时间中的变化，而不是单个胜者卡片。
5. 把 Decide 展示为简短决策解释：是否需要切、是否执行、如果没执行是被什么挡住。

## Accessibility & Inclusion

- 中文优先完成信息架构和文案，中文稳定后再迁移英文。
- 明暗主题都需要保持清晰可读。
- 颜色不能成为唯一的信息编码；关键状态需要同时使用文字、图标或结构提示。
- 动效应服务状态变化和层级理解，并尊重 reduced motion。
